require([
  /* Application object */
  'application',
],
function (App) {
  'use strict';
  /* Start the Application Object. You can optionally add options to the the
   * start function. These options will be available to the initialize 
   * functions and events.
   *
   * [https://github.com/marionettejs/backbone.marionette/blob/master/docs/marionette.application.md#starting-an-application]
   */
  App.start();
});
